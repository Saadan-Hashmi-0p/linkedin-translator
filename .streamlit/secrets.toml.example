# Copy this file to secrets.toml (same folder) for LOCAL development only.
# Never commit the real secrets.toml — it's already in .gitignore.
#
# On Streamlit Community Cloud, you don't need this file at all —
# set HF_TOKEN under your app's Settings > Secrets instead.

HF_TOKEN = "hf_xxxxxxxxxxxxxxxxxxxx"
